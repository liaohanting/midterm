#include<stdio.h>
main()
{
    char c[50];
    int a=0,0=0;
    printf(c\n);
    while((c = getchar()) !='\n'
        if (c=='a'||c='A')
        a++;
        else if(c='0')
        0++;
    printf("The numer of a is: %d and number of 0 is: %d\n")
}